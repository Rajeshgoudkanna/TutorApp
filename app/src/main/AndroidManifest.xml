<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tutorapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TutorApp">
        <activity android:name=".Activities.TutorViewRating" />
        <activity android:name=".Activities.StudentRating" />
        <activity android:name=".Activities.CourseReviewActivity"/>
        <activity android:name=".Activities.CourseReviewsActivity"/>
        <activity android:name=".Activities.UserCourseViewActivity"></activity>
        <activity android:name=".Activities.TutorViewCoursesActivity" />
        <activity android:name=".Activities.CourseActivity" />
        <activity android:name=".Activities.CourseDetailsActivity" />
        <activity android:name=".Activities.MyProfileActivity" />
        <activity android:name=".Activities.ChangePasswordActivity" />
        <activity android:name=".main_tutor" />
        <activity android:name=".registration" />
        <activity android:name=".login" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>